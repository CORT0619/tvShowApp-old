language: node_js
node_js:
- "7"

install:
  - npm install -g @angular/cli
  - npm install

script:
  - ng build

deploy:
  provider: s3
  access_key_id: ""
  secret_access_key: ""
  bucket: ""
  skip_cleanup: true
